# Minimal Model Data

The exact manuscript PDFs for the minimal two-orbital model are included under
`figures/paper/`:

- `sigma_axis_model_1st_nn.pdf`
- `sigma_axis_model_2nd_nn.pdf`

The compact CSV data for the model scans still need to be extracted or
regenerated from the minimal model workflow. The expected follow-up file is:

```text
data/processed/minimal_model/model_sigma_axis.csv
```

The repository keeps the exact published figures now, while leaving model-data
regeneration as a documented next step.
