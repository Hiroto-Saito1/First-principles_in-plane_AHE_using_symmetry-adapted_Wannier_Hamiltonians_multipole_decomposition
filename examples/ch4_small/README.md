# CH4 Small Fixture

This example slot is reserved for a compact molecular fixture copied or
reduced from the old working repository. It should remain small enough for
regular `pytest` runs and should document its provenance when added.

